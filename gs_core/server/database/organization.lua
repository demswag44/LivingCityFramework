---------------------------------------------------------------------
-- GS Framework
--
-- Organization Repository
---------------------------------------------------------------------

GS = GS or {}

GS.Database.Organization = {}

local Organization = GS.Database.Organization

----------------------------------------------------
-- Load
----------------------------------------------------

function Organization.Load(id)

    GS.Logger.Debug(
        "DATABASE",
        ("Load Organization %s"):format(id)
    )

    -- SQL will be added later

end

----------------------------------------------------
-- Save
----------------------------------------------------

function Organization.Save(id)

    GS.Logger.Debug(
        "DATABASE",
        ("Save Organization %s"):format(id)
    )

    -- SQL will be added later

end